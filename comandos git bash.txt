git status (enter) para visulaizar o que foi alterado na pasta local

git add . (enter) para adicionar as alterações

git status (enter) para ver os arquivos detalhados

git commit -m "estrutura do site" (enter)

git status (enter) para ver se está tudo ok

git push (enter) sincronizar a pasta local com o github

git pull (enter) sincronizar github com a pasta local

That's all!!

